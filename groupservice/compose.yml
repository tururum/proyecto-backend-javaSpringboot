version: "3.8"

services:
  mysql:
    image: mysql:latest
    container_name: mysql-groupservice
    environment:
      MYSQL_ROOT_PASSWORD: Karma30102001.
      # uncomment below if you want to automatically create and select a database
      # MYSQL_DATABASE: my_database
    ports:
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data: