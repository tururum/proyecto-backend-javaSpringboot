info:
  name: DeleteUser
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{studentservice}}/students/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
