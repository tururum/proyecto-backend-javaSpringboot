info:
  name: UpdateStudent
  type: http
  seq: 4

http:
  method: PUT
  url: "{{studentservice}}/students/1"
  body:
    type: json
    data: |-
      {
        "telefono" : "84824199"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
