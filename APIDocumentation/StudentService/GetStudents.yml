info:
  name: GetStudents
  type: http
  seq: 1

http:
  method: GET
  url: "{{studentservice}}/students"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
